export const message = {
  noConfig:
    'There is no configuration object. Please refer to the documentation and pass a configuration object as an argument.',
  noContainer: 'Cannot find the container element.',
} as const;
